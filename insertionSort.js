function insertionSort(arr) {
    for (let i = 0; i < arr.length; i++) {
        for (let j = 0; j < arr.length; j++) {
            while (arr[j] >= arr[i]) {
                arr[j] === arr[i];
            }
        }        
    }
    return arr[i];
}

